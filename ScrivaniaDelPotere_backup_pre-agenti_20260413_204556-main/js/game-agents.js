// game-agents.js — Sistema Agenti Autonomi
// Placeholder: inserire qui il codice completo delle classi agenti e orchestratore.
// Per il codice completo (~500 righe), fornire il contenuto o confermare permessi push.

// Esempio di auto-inizializzazione:
(function initAgentSystem() {
    if (typeof AgentMaster !== 'undefined' && typeof AgentMaster.autoInit === 'function') {
        AgentMaster.autoInit();
    }
})();
